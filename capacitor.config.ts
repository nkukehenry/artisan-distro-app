import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.somesa.elearning',
  appName: 'Somesa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
