import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.solvertech.artisandistro',
  appName: 'Artisan Distro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
